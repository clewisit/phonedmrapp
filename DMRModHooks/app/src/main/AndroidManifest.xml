<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dmrmod.hooks">

    <!-- Storage permissions for CSV backup/restore -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- Network permission for elevation API and geocoding -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location permissions for distance calculation -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_preferences">

        <!-- LSPosed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Comprehensive modifications for PriInterPhone DMR app" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <!-- BackupActivity for OpenGD77 CSV export/import -->
        <activity
            android:name=".BackupActivity"
            android:label="DMR Backup &amp; Restore"
            android:theme="@android:style/Theme.Material.Light"
            android:exported="true" />

        <!-- APRSSettingsActivity for APRS configuration -->
        <activity
            android:name=".APRSSettingsActivity"
            android:label="APRS Settings"
            android:theme="@android:style/Theme.Material.Light"
            android:exported="false" />

    </application>

</manifest>
